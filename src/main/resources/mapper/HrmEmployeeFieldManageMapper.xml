<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tianye.hrsystem.mapper.HrmEmployeeFieldManageMapper">
    <select id="queryEmployeeManageField" resultType="com.tianye.hrsystem.entity.vo.EmployeeFieldManageVO">
        SELECT a.id,a.entry_status, b.field_id,b.field_name,b.`name`,b.label,b.label_group,a.is_manage_visible
        from hrm_employee_field_manage a INNER JOIN hrm_employee_field b on a.field_name=b.field_name
        WHERE
        b.type != 50
        <if test="null != entryStatus">
            and a.entry_status=#{entryStatus}
        </if>
        <if test="null != label">
            and b.label=#{label}
        </if>
        ORDER BY b.label_group,b.sorting
    </select>
</mapper>
