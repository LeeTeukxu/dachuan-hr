# 考勤同步（钉钉 sync/syncAll / 打卡 / 考勤组 / 审批数据获取 / 每月 1 号集中同步）

菜单前缀：`/attendanceData`（同步链路）、`/hrmAttendanceApproval`（审批数据）、`/hrmAttendanceEmpMonthRecord`（打卡概况）、`/dingTalkApiUsage`。

## 需求要点
- 同步入口 `/attendanceData/sync|syncAll` 是唯一允许调钉钉并落库的边界；展示查询（排班管理、打卡记录、审批数据列表）只读本地快照，禁止实时调钉钉。
- 审批数据：数据源限定本地 `tbattendanceapprove`；手工"获取审批数据"按 月份→员工→审批实例→详情 拉取，只入库 `status=COMPLETED && result=agree` 的实例；重抓按员工清理陈旧快照（基于 `employeeId→userId`，禁按姓名）；支持手工添加/删除/时长与子类型修正/统计状态（`取消至统计`）行内维护。
- 员工映射：禁按"同名+无排班"破坏性清理；去重以 userId 为主键；员工表 `dingtalk_user_id` 需经钉钉通讯录校验，缺失/失效按"姓名+手机号"唯一反查后写回。
- 钉钉调用量：`/dingTalkApiUsage/monthly` 读本地 `postresultlog` 统计（月额度 `dingtalk.api.monthly-limit` 默认 10000、阈值 80%），不在线请求钉钉。
- 每月 1 号集中同步（硬性要求）：≤8 家公司同一时间触发同步/审批获取，各公司数据必须落各公司租户库，互不干扰、不允许排队等待；同公司重复触发当场拒绝。

## 设计与契约
- 钉钉接口与落库表详细说明：`../reference/钉钉接口及数据库.md`（业务阅读版以同名 `.xlsx` 为准）。
- **部署依赖 SQL**：存量租户库需执行 `../sql/2026-09-05_tbattendanceapprove_duration_day.sql`（`tbattendanceapprove.durationDay` 时长(天)列；**推荐整库执行一次全租户遍历版 `../sql/2026-09-05_tbattendanceapprove_duration_day_all_tenants.sql`**，自动遍历全部 hr_数字 库、幂等可重跑——租户库缺该列时考勤汇总-下载行政体系考勤报 `Unknown column 'tbattendan0_.durationDay'`，2026-09-07 生产即因漏执行此脚本触发）。
- 7 步链路（`HrmAttendanceDataServiceImpl#SyncDataWithResume`）：①考勤组+班次（`hrm_attendance_group(_relation_*)`、`hrm_attendance_shift`，快照整体替换、先拉全量成功才落库）→②用户映射（`tbattendanceuser`）→③排班计划（`hrm_attendance_plan`）→④打卡明细（`hrm_attendance_clock`+`tbattendancedetail`）→⑤报表字段→⑥删旧报表→⑦报表数据回灌（`hrm_attendance_report_data`）。`date_shift/emp_schedule/history_shift` 不属于本链路直写。
- 审批抓取：按目标员工 `process/listbyuserid` 解析流程编码（管理员模板已退出主链路）→ `processinstance/listids` → `processinstance/get` → 解析落库；需应用权限 `qyapi_aflow`。**月/窗双模式**：老入口/定时任务=月模式，按目标业务月拉取并对业务日期二次过滤所选月份；前端手动=窗口模式（带 `fetchStartTime/fetchEndTime`），listids 按发起时间窗口 ≤120 天分片拉取、已通过审批全量幂等落库、不再按业务月丢弃（跨月单修复），列表/统计仍按业务 `beginTime` 过滤。完成标记表 `hrm_attendance_approval_fetch_mark`（不再作前置拦截）。
- 集中同步方案（已实施，源文档 `../archive/考勤同步并发风险与优化方案-2026-08-30.md`）：接口提交路径同步抢占按公司锁后立即返回 `{queued:true, queuedAt}`——考勤锁 Redis `attendance:sync:running:<companyId>`（TTL 2h，finally 释放），审批为 JVM 内按公司标记（2h 陈旧自动放行）；全部 11 个进度键追加 `:companyId`；托管线程池 `AttendanceSyncTaskLauncher` 核心=上限=8/队列 8，AbortPolicy 队列满返回"繁忙"；失败自动重试（默认 2 次，指数退避 60s→5min，考勤断点续传、审批幂等重拉；参数类/数据类错误不重试），前端轮询上限 60 分钟。浏览器 F5 只中断旧页面轮询，不取消后台任务、不释放公司锁；新页面加载不查询进度，用户再次点击"同步考勤"时才按当前公司查询，检测到 `RUNNING` 后恢复进度弹窗。明确不做：不拆微服务、不分库、不改内部限流参数（PARALLEL_THREADS=2、50 人批量、100ms 节流）。
- 钉钉限流对照（8 家并发）：每应用 20 QPS 独立不叠加；同源 IP ≈500 QPS 上限余量 ≥5 倍；接口全局池阈值不可证实但有退避+断点兜底；月配额每应用独立，靠 80% 用量提醒。
- 打卡概况：历史月不得用 `hrm_employee.create_time` 拦截；缺卡补 `-3`；同日多次打卡返回完整时间线；月查询不固定 `clock_stage=1`。

## 近期变更
- 2026-09-07 本地考勤判定引擎上线（弃用钉钉排班推送）：`hrm.dingtalk.schedule-push.enabled` 默认 false，排班只落本地 tbplanlist；新引擎 `HrmAttendanceJudgeServiceImpl` 以 tbplanlist 为应出勤基准（夜班=跨天、宽松取卡、迟到缺卡互斥、旷工=全天无卡）判定迟到/早退/缺卡/旷工，结果落 `hrm_attendance_judge_result`（新表）；补卡取审批数据（tagName 含"补卡"），每人每月前 N 张生效（考勤规则 `max_monthly_card_repair`，超限单不参与判定）；阈值/窗口取考勤规则新列 judge_window_before/after_minutes；同步考勤完成后自动重算，`/attendanceData/judgeRecompute` 手动重算；员工保存钩子（add/改姓名/改手机号）按姓名+手机号映射 dingtalk_user_id，查无此人拒保存，`DingTalkUserMappingRetryTask` 每日补齐 + `/hrmEmployee/remapDingTalkUser` 手动；排班域代码不再读写 tbattendanceuser（读侧双键兼容历史 employeeId 键行），考勤域保留。SQL：`docs/sql/2026-09-06_attendance_judge.sql`（租户库，生产已执行）。结果展示：考勤汇总页新增"本地判定"列组（按月聚合）与「重算本地判定」按钮，查询接口 `/attendanceData/judgeQuery`。详见 `miniapp/docs/完成情况.md` 第二十七轮与 `reference/系统功能修改Prompt清单.md` 7.13。
- 2026-09-06 修复「获取审批数据/同步考勤」进度弹窗假完成（进度条瞬间 100% 但后台仍在拉取）。**根因**：前端先开进度轮询、后发提交请求，第一拍读到上次运行残留在 Redis 的旧完成态（`attendance:fetch:<companyId>` TTL 24h，关闭弹窗不清除），`useProgressPolling` 见 `done=true` 即停轮询；通知中心由后台任务直写通知故正常。**修复**（仅前端）：①`approval/Index.vue`、`employee/Index.vue` 改为弹窗先归零、轮询挪到 `fetchMonthData`/`attendanceDataSync` 响应返回后才开始（此时后端 `beginFetchProgress` 已覆盖为本次 RUNNING）；②`useProgressPolling.js` 加防呆——本地从未观察到 RUNNING 就读到的完成态不当作完成，20s 宽限期内继续轮询等真实状态，防止以后新页面再写出"先轮询后请求"的顺序。薪资/排班/保险的进度时序本就正确未动。后端无改动；未加清进度接口（残留状态靠时序+防呆规避）。**连带修复**：时序修正暴露的等待循环卡死——`pollFetchUntilDone`/`waitForSyncCompletion` 在用户中途关闭进度弹窗（轮询停止、done 被重置）后会空转到超时，期间提交按钮一直 loading；两处循环均加 `!isPolling` 立即退出，`!completed` 时不再刷新列表。
- 2026-09-05 跨月请假单（调休/年假）按月拆分显示 + 请假日期解析加固。**背景**：7/24 申请、业务 7/24~8/4 的调休单此前在 8 月列表查不到（L1 区间交集修复后，hr_0001 存量数据与 SQL 已验证 8 月命中；故障现场疑似旧版本部署）。**本轮**：①`Parser#resolveDateRange` 新增 `resolveLeaveDateRange`——请假类审批优先从请假控件 `ext_value` 权威 `start_time`/`end_time`(毫秒) 取业务区间，其次"请假时间/假期"等请假命名组件 value 日期，最后全组件日期扫描 min/max 兜底；修复请假控件组件名不在通用候选名单时 beginTime/endTime 兜底成发起/完成时间、跨月单整段落发起月的问题。②`HrmAttendanceApprovalServiceImpl#queryPageList` 增加展示层按月拆分 `clampCrossMonthRowsForDisplay`：审批区间与所选月部分相交时，展示的 beginTime/endTime 截断为该月内区间（结束时间保留单的真实结束日）。时长折算：**按天请假（总时长为 8h 整数倍）按整天累加**——月部分=该月工作日数×8h（封顶原单总时长），天数恒为整数；按小时请假退化为工作日占比折算（0.5h 粒度）。工作日判定按员工属性路由（`hrm_employee.affiliation_system`/`rest_type`）：**行政体系(1)+行政单双休(1)的固定工时员工直接走单双休日历**（不查排班），其余员工（固定月休4天/生产体系等）先剔除排班休息日（`tbplanlist` `shift_source='rest'` 且 UserID 含员工钉钉ID），排了班次的日期计工作日，未覆盖日期回落日历 `HrmWorkweekSettingService#countWorkDays(start,end,scheduledDayStatus)`（日历判定链=手动日设置>调休上班>法定休息>周休、未初始化按周一~五）；拆分计算已提为公开口径入口 `IHrmAttendanceApprovalService#calculateMonthPortion(employeeId,beginTime,endTime,duration,durationDay,month)`（返回 `AttendanceApprovalMonthPortionVO`：该月展示区间/时长/天数/工作日数，现算不落库），列表与后续统计模块统一调它，保证口径唯一；仅改展示，库内单条完整记录、统计口径不变（统计侧按自然月切分仍待排期）。ParserTest 新增跨月"请假时间"组件与 ext_value 区间 2 用例，相关 4 测试类 80 全绿。无表/接口变更、不涉 /mp 契约。**验收状态**：员工"所属体系/休息制度"已在服务器补填待页面点验（预期陈潜 8 月=8/1~8/4、2 天/16h；本地库 290 人该两字段仍为空，空值回落单双休日历不影响判定）。
- 2026-09-05 修复「请假(尤其半天年假)审批时长解析错误」+ 时长"天"口径持久化。**根因**（孙蕃荣 8/24、8/25 各请下午半天年假，钉钉 时长(天)=0.5 复现）：钉钉请假控件 ext_value 对"按天填写"的请假，`durationInHour` 返回**日历小时伪值**（下午半天=固定 12h），而权威"时长(天)"在 `durationInDay`(0.5)；旧解析**先信 durationInHour** 错存 12 小时≈1.5 天。**修复**：`resolveComplexLeaveDuration` 判定 `durationInDay` 是 0.5 的整数倍(=按天填写，半天/全天/1.5天等)⇒ 小时=durationInDay×8(8 工作小时/天)落库；否则为按小时填写(如30分钟→durationInDay=0.07)才用 durationInHour。半天 0.5→4h。**持久化**：表新增派生列 `durationDay`(恒=小时/8)，抓取/手动新增/行内改三入口同步写，列表接口/前端独立"天数"列展示并可改(改小时自动算天数、改天数反算小时提交)。改文件：entity/`schema-baseline.sql`/`docs/sql/2026-09-05_tbattendanceapprove_duration_day.sql`/Parser/ServiceImpl/Mapper/VO/`AttendanceRecordManager` + 前端 Index.vue；ParserTest 新增半天年假与 1.5 天两用例(10 全绿)。既有脏单(2 条 12h)按方案 A 待重抓覆盖。
- 2026-09-04 自动同步定时任务（默认关）+ 跨月抓取丢数据修复（V4）+ 农谷连接修复：①`DingTalkAutoSyncConfig`/`DingTalkAttendanceAutoSyncTask`/`DingTalkApprovalAutoSyncTask`（有界线程池、复用公司互斥锁），任务进行中接口返回 202；②「获取审批数据」新增**发起窗口入口**（5 参 `fetchMonthData(month,start,end,empIds,types)`，前端锁终点+开始日期弹窗），窗口内已通过审批全量幂等落库、不再按业务月丢弃（发起月≠业务月的单不再永久丢失），落库与展示解耦，详见 `../plans/2026-09-04-attendance-approval-cross-month-fetch-review.md`；③`tbattendanceapprove`/`tbattendancerecord`.`userId` 扩到 varchar(100)+补 openid（农谷 Hr_0003 修复）。

## 历史摘要
- 2026-09-05：撤销审批三轮修复定稿——`isApprovedProcessInstance` 三大作废判据齐备（撤销动作单/REVOKE+mainProcessInstanceId 替身/attached 非空被顶替原单），任一命中不入库并删残留快照；用户定稿"两层（业务级+单条审核）均通过才入库"。
- 2026-09-04 自动同步定时任务（默认关）+ 跨月抓取丢数据修复（V4）+ 农谷连接修复：①`DingTalkAutoSyncConfig`/`DingTalkAttendanceAutoSyncTask`/`DingTalkApprovalAutoSyncTask`（有界线程池、复用公司互斥锁），任务进行中接口返回 202；②「获取审批数据」新增**发起窗口入口**（5 参 `fetchMonthData(month,start,end,empIds,types)`，前端锁终点+开始日期弹窗），窗口内已通过审批全量幂等落库、不再按业务月丢弃（发起月≠业务月的单不再永久丢失），落库与展示解耦，详见 `../plans/2026-09-04-attendance-approval-cross-month-fetch-review.md`；③`tbattendanceapprove`/`tbattendancerecord`.`userId` 扩到 varchar(100)+补 openid（农谷 Hr_0003 修复）。
- 2026-09-02~03：通知中心实时进度、进度提前 100% 修复、三入口锁释放增强、Hikari 防驱逐；农谷公司 userId 列过窄/openid 缺失修复（全租户 `MODIFY COLUMN userId VARCHAR(100)` + 补 `openid`，baseline 已改）。
- 2026-08-30~31：每月 1 号集中同步实施（按公司锁 + 进度键隔离 + `AttendanceSyncTaskLauncher`）；排班分页节流 100ms sleep 峰值压至 ≈10 QPS；失败自动重试（指数退避 60s→5min）。
